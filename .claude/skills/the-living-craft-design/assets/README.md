No brand assets were supplied with the brief — no logo, no mark, no imagery, no font binaries.

Nothing has been drawn or reconstructed to fill the gap. Wherever a logo would go, the wordmark
"The Living Craft" is set in the active direction's display face (see `guidelines/brand-wordmark.html`).

Drop real assets here when they exist:
- `logo.svg` — the mark, if one is ever made
- font binaries — then replace the CDN `@import`s in `tokens/fonts.css` with local `@font-face` rules
