Every agent claim that came from a file, run, or page carries one. Uncited agent claims should look wrong.

```jsx
<Citation n={4} source="queue/config.yaml:31 · read at run 118">the queue is configured for at-least-once delivery</Citation>
```
