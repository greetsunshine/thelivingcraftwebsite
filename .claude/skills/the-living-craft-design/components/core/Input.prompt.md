Text entry. Wrap in `Field` for a label, hint or error.

```jsx
<Field label="Repository" hint="Read-only access is enough.">
  <Input mono placeholder="github.com/org/repo" />
</Field>
<Input as="textarea" placeholder="What failed in production?" />
```
