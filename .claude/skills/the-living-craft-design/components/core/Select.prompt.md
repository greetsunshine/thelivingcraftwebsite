Use for closed sets of 4+ options; below that use Radio-style buttons or a segmented row of `Button`s.

```jsx
<Field label="Cohort"><Select options={["Cohort 04 — Sep", "Cohort 05 — Nov"]} /></Field>
```
