For a surface with no content yet. Write the sentence a person would say.

```jsx
<EmptyState index="§ 05" label="No findings on this pull request." note="The reviewer agent runs on push; nothing has been pushed since Tuesday." action={<Button size="sm">Run it now</Button>} />
```
