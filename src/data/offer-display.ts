// Approved public offer facts shared by every visitor-facing surface.
//
// The 11 September V4 copy intentionally does not publish a fee, start date or
// week count. Keep that policy in data rather than behind a presentation flag:
// the page, structured data, public API, /llms.txt and Q&A assistant must all
// give the same answer.

/** The only published time commitment in the delivered copy. */
export const COMMITMENT = '30 live hours plus independent work.';

/** A target, not a capacity limit or scarcity claim. */
export const COHORT_SIZE = 'The open cohort targets eight members.';

/** Compact forms for comparison-table cells. */
export const COMMITMENT_CELL = '30 live hours, plus independent work';
export const COHORT_SIZE_CELL = 'Targets eight members';

/** The public answer wherever a visitor asks about schedule or price. */
export const FEES_NOTE =
  'Fees, schedule, payment, refund and access terms are confirmed in the final offer, before you commit to anything. Applying starts that conversation.';
